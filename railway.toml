[build]
builder = "nixpacks"

[build.setup]
nixPkgs = ["python310Full", "gcc"]

[install]
commands = ["pip install -r requirements.txt"]

[start]
command = "uvicorn api.main:app --host 0.0.0.0 --port 8000 & streamlit run frontend/app.py --server.port $PORT --server.address 0.0.0.0"
